.tabs {}

.tabs_title {
  display: flex;
}

.title_item {
  display: flex;
  padding: 15rpx 0;
  justify-content: center;
  align-items: center;
  flex: 1;
}

.active {
  color: var(--themeColor);
  border-bottom: 5rpx solid currentColor;
}